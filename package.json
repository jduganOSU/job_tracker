{
  "name": "job_tracker",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install:front_end": "cd frontend && npm install",
    "install:server": "cd backend && npm install",
    "install:all": "npm-run-all --parallel install:front_end install:server",
    "start:front_end": "cd frontend && npm start",
    "start:server": "cd backend && npm start",
    "start:all": "concurrently \"npm run start:server\" \"npm run start:front_end\"",
    "install:project": "npm install && npm run install:all",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --prefix frontend && npm run build --prefix frontend && npm install --prefix backend"

  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "axios": "^1.6.8",
    "express": "^4.19.2",
    "mongoose": "^8.3.5",
    "react-fa": "^5.0.0",
    "react-icons": "^5.2.1"
  }
}
